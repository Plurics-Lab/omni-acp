{
  "name": "@omni-acp/compat-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Config-driven compat suite: the same SDK script against every configured agent.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@omni-acp/client": "workspace:*",
    "@omni-acp/daemon": "workspace:*",
    "@omni-acp/protocol": "workspace:*",
    "@omni-acp/testkit": "workspace:*",
    "yaml": "^2.9.0"
  }
}
