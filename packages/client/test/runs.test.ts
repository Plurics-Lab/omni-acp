import { describe, it } from "vitest";

/**
 * `server.runs` — DESIGN §9.3's fire-and-forget half of the SDK.
 *
 * Owned by M2-B-WP-R.
 */

describe("server.runs", () => {
  it.todo("events() is the SAME resumable tail worker.events() uses, pointed at the run's route");
});
