/**
 * @omni-acp/cli — FROZEN by the scaffold (M0-PLAN.md §1.2): re-export only.
 * The executable is `./dist/bin.js`; everything testable lives behind these three names.
 */
export { parseArgs, type ParsedArgs } from "./args.js";
export { yamlToDaemonConfig } from "./yaml-config.js";
export { main } from "./main.js";
